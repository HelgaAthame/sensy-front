# For start project

## Add `.env.local` file in root folder with the following content:

```
NEXT_PUBLIC_BASE_API_URL='http://86.57.195.162:5187/api/'
```

## Install `pnpm` version specified in `package.json`